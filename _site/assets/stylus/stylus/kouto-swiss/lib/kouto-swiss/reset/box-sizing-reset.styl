ks-box-sizing-reset()
    if selector() is "&"
        html
            box-sizing: border-box

        *, *:before, *:after
            box-sizing: inherit
    else
        *, *:before, *:after
            box-sizing: border-box

box-sizing-reset = ks-box-sizing-reset unless ks-no-conflict
